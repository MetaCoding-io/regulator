/** The lab's process runner is the checks package's: one `Exec` shape for every Pi-free module. */
export { realExec, type Exec, type ExecOptions, type ExecResult } from "@metacoding/regulator-checks";
