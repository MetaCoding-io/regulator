A JSON file here is a payment the household has agreed to make: payee, amount, dueDate,
category, memo. Writing one is a commitment, so a unit asks for consent first and
writes nothing without a yes. The person moves the record to `../executed/` when paid.
