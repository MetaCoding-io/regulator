{
  "name": "@metacoding/regulator-checks",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": false,
  "description": "Deterministic S3* checks: host-run verification bound to a revision, and the technical verdict derived from it.",
  "homepage": "https://github.com/MetaCoding-io/regulator#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MetaCoding-io/regulator.git",
    "directory": "packages/checks"
  },
  "publishConfig": {
    "access": "public"
  },
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node --test dist/*.test.js"
  },
  "dependencies": {
    "@metacoding/regulator-protocol": "workspace:*"
  }
}
