{
  "id": "reg.coordination.thrash-detector.v1",
  "name": "Thrash detector",
  "status": "active",
  "vsmFunction": "S2",
  "purpose": "Count write and edit calls per file within a unit and emit a typed coordination signal past a threshold, so oscillation between two fixes becomes visible to S3 instead of burning budget silently.",
  "absorbs": {
    "failureClass": "oscillation",
    "description": "Fix A breaks B, fix B breaks A; each individual edit is locally reasonable and nothing in the loop notices the pattern."
  },
  "mechanism": {
    "level": "deterministic-gate",
    "implementation": "src/cp4-coordination.ts",
    "enforcementPoints": ["tool_execution_end"]
  },
  "channels": { "consumes": ["tool_execution_end (write, edit)"], "emits": ["coordination-signal (oscillation) → S3"] },
  "scope": { "subjects": ["unit"], "resources": ["file"] },
  "authority": {
    "may": ["record a coordination signal to .regulator/signals.ndjson and notify the user"],
    "mayNot": ["block a tool call", "pause or replan the unit (S3 decides, lesson 08)", "decide which of two fixes is right"]
  },
  "evidence": { "tests": ["src/cp4-coordination.test.ts", "../../packages/core/src/coordination.test.ts"] },
  "limitations": [
    "Counts write and edit tool calls only; edits made through bash are invisible to it.",
    "Memory is per session: a unit resumed in a new session starts counting from zero.",
    "The threshold is the policy's `coordination.oscillationThreshold` since lesson 12 (4 when a policy declares none); it is one number for every file and unit type, not a budget that varies by kind of work."
  ],
  "ownership": { "owner": "course-lab", "introduced": "2026-09-22", "reviewBy": "2026-12-01" },
  "ablation": {"switch": "extension:cp4-coordination", "note": "Shares checkpoint 4 with the lease and worktree mechanics in the session; the oscillation fixture is the suite. The loop's attempt ceiling still stops a thrashing unit, later."},
  "retirement": {"condition": "No oscillation in the oscillation fixture across three model versions with the detector off: the models stop alternating on their own, or the ceiling catches it at acceptable cost."},
  "introducedIn": "M05"
}
