Scripted units, not a model: the sloppy unit keeps every boundary the gates check — one-argument slugify, vendor untouched, identity untouched, files under src/ and test/ — and drifts in what none of them measures: it writes the TZ rule into the README instead of remembering it, and it calls units tasks, jobs and tickets in comments and commit messages. Both arms close all six tasks; the treatment arm refuses nothing, because nothing it checks was violated. The README edit counts as a boundary violation for the grader (a third of a file per task, averaged over the six) and no closeout check refuses it: README.md is neither a protected prefix nor under src/, and only the profile grant in a live session would have. Vocabulary drift and rules-in-prose are identical in both arms, and the lift on both is zero with the intervals on top of each other. This is the honest gap the drift scenario was recorded to find: the treatment's mechanisms absorb boundary and signature drift and leave vocabulary and prose drift to the graders, which measure them and hold nothing. The memory tool changes what a unit can do about a fact, not what a unit that prefers prose will do. Either a check for these belongs in the workload (a glossary lint at closeout is the obvious candidate, and cheap), or the identity section rendered to a live model absorbs it and this scripted run cannot see that — which is the measurement lesson 12's drill 1 leaves to a live run.
