---
aside: false
---

# Build debt

What the build knows it has not yet absorbed. One row per gap that is *debt* — taken on
deliberately, with a place it will be paid — as opposed to a permanent limit of a
mechanism. The registry cards under `packages/regulator/registry/regulators/` remain the source
of truth for what each regulator does not catch; this file indexes the subset that is
owed, so nothing is stated twice and nothing is stated only in a card's fourth bullet.

Rules:

- A new registry limitation that names a later lesson, milestone or "not yet" gets a row
  here in the same change (AGENTS.md, "Before finishing an issue").
- A row is closed by the change that pays it: strike the row, cite the commit or PR, and
  update the card's limitation to say what the mechanism now does.
- "Resolves in" is the issue and milestone [`ROADMAP.md`](../ROADMAP.md) assigns, not a
  promise of a date. A row with no issue says *unscheduled*, and the roadmap's "Not
  planned" table says why when the reason is the deployment's.

| # | Item | Recorded in | Resolves in | Taken on |
| --- | --- | --- | --- | --- |
| ~~1~~ | ~~The audit log is an NDJSON file (`.regulator/audit.ndjson`) beside the regulatory log (`.regulator/signals.ndjson`), and neither is the SQLite regulatory event store the reporting tools write (`.gsd/vsm-runtime/vsm.db`), whose path still carries a GSD-era name. Lesson 11 decided that obligations fold from events in the same log as the messages they route, so the instance has two NDJSON histories, not three; absorbing them into one store is the observability work.~~ Paid in lesson 14 by projection, not by merging: `projectSpans` reads every store the instance keeps into one set of OpenTelemetry GenAI spans, correlated by unit, attempt, evidence, obligation, regulator and policy version, with redaction. The stores stay separate; the SQLite store and its path are row 31. | `span-projection`; `closeout-gate` limitation 4 | — | lesson 09 → narrowed lesson 11 → paid lesson 14 |
| ~~2~~ | ~~Evidence binds to contract criteria by class, not by content: a passing suite that does not exercise the changed behaviour satisfies a test-class criterion. The software workload declares `run_tests`, `run_checks` and, since lesson 12, `identity-untouched`; a behaviour-bound check is still a workload's to declare, and the drift scenario's signature check is the first one planned.~~ Paid in lesson 14: an evidence expectation may carry a check (`export-signature`), the host observes it by content at HEAD, and the record binds to that criterion alone. Binding by class remains the default for expectations that carry no check; one kind of check exists (row 35). | `behaviour-check`; `closeout-gate` limitation 1 | — | lesson 09 → rescheduled lesson 12 → paid lesson 14 |
| ~~3~~ | ~~Runtime-class evidence is treated like semantic evidence (human acceptance) because no host mechanism observes runtime behaviour.~~ Paid in lesson 14: a runtime criterion with fresh host evidence from a content check is satisfied by it, and a person is asked only where no probe exists. | `behaviour-check`; `closeout-gate` limitation 3 | — | lesson 09 → rescheduled lesson 12 → paid lesson 14 |
| 4 | The environment an evidence record carries is the orchestrator's host; a check that passes here and fails on the target platform is not caught. | `closeout-gate` limitation 2 | unscheduled | lesson 09 |
| ~~5~~ | ~~Remediate, replan, clarify, pause and escalate are recorded and the unit waits; nothing performs them, reminds anyone, or delivers the algedonic signal beyond the read model. Policy proposals (lesson 10) land in the same sink with the same fate.~~ Paid in lesson 11: each waiting action opens an obligation owed to the consumer the routing policy names, proposals and escalations are obligations owed to S5 or a person, the veto holds the unit until a disposition, and the disposition reaches the next attempt. Delivery and reminders remain row 20. | `recovery-router` limitations 2 and 4; `proposal-intake` limitation 1 | — | lesson 08 → paid lesson 11 |
| ~~6~~ | ~~Capability profiles are code (`packages/regulator/src/profiles.ts`), not a declared part of the definition; `regulator status` reports them as pending.~~ Paid in lesson 12: profiles are files under `packages/regulator/profiles/`, validated on load and by `regulator check`; the read model declares them. | `course/CONTROL-REGISTRY.md` status; `packages/cli/src/status.ts` | — | lesson 06 → paid lesson 12 |
| ~~7~~ | ~~Path gates are lexical: no symlink, hard-link, absolute-path or TOCTOU protection in the vendor write gate, the profile write grant, or the lease's coverage of the working directory.~~ Paid in lesson 10: `prepareWritePath` (core) walks the filesystem for aliases and refuses absolute paths and traversal; checkpoint 9's gate supersedes the lexical ones when loaded. TOCTOU between the walk and the write remains a stated limit, not debt. | `identity-write-gate` limitations 1–3 | — | lesson 02 → paid lesson 10 |
| 8 | `bash` is outside most mechanisms: invisible to the thrash detector, the effect journal, and the failure observer's non-error exits; the implement profile grants it un-gated. Lesson 10 added the snapshot-and-restore for protected paths and lesson 12 the branch diff at closeout, which narrow this to non-protected, non-committed effects. Lesson 15 decides the shell's reach is the container's to declare (OPERATING.md § Security posture) and the manifest's declared prefixes bind `write` and `edit` only. | `thrash-detector` limitation 1; `profile-write-grant` limitation 1; `effect-journal` limitation 3; `failure-observer` limitation 2 | unscheduled (real isolation is the operating system's or a container's; the lab documents it and does not build it) | lesson 03 → rescheduled lesson 12 → rescheduled lesson 12 → unscheduled lesson 15 |
| 9 | Leases and the effect journal are files on one machine: no fencing token, no cross-host coordination, and two harnesses on two machines cannot see each other's intentions. | `unit-lease` limitations 1 and 3; `effect-journal` limitation 4 | unscheduled (deferred with full recursion) | lesson 05 |
| ~~10~~ | ~~Authority references on fixed decisions are strings; nothing verifies that the cited invariant or decision exists.~~ Paid in lesson 12: a reference resolves to an invariant the instance's identity declares, a regulator the registry declares, an obligation the instance holds, or a named person, or the contract is refused. A person's name is trusted as given (row 20). | `work-contract-gate` limitation 2 | — | lesson 06 → paid lesson 12 |
| ~~11~~ | ~~Reintegration detects file-level conflicts only; two units can change disjoint files and break each other, and nothing runs the checks on the merged base. The closeout gate verifies the unit's branch at its HEAD, not the merge result.~~ Paid in lesson 15: after every reintegration the loop runs `run_tests` and `run_checks` on the base at the merge commit, records the evidence, and a failure is an audit finding with no unit — an obligation on the instance itself that holds every dispatch until S3 dispositions it. It runs after the merge, not before (row 37); file-level conflict detection is unchanged. | `post-merge-check`; `reintegration` limitations 1 and 3 | — | lesson 05 → rescheduled lesson 12 → rescheduled lesson 14 → paid lesson 15 |
| ~~12~~ | ~~Emergent decisions below high consequence, and residual uncertainty, are recorded in the report only; nothing routes them.~~ Paid in lesson 11: every emergent decision, deviation and residual uncertainty becomes a signal, and the routing policy decides which open an obligation and which are noted as trace with the reason. | `result-report-gate` limitation 3 | — | lesson 06 → paid lesson 11 |
| 13 | The fixture lacks the flaky test and the migration that must not be re-run; drills that need them (lessons 08 and 09) describe the setup by hand. | `course/PRODUCTION-PLAN.md`, next actions | [#55](https://github.com/MetaCoding-io/regulator/issues/55) (roadmap 0.2); was: Phase 2 fixture work | lesson 05 |
| 14 | The thrash detector's memory is per session; a resumed attempt starts counting from zero. Lesson 11 gave the signal a history and lesson 12 made the threshold the policy's (`coordination.oscillationThreshold`); the count itself still restarts. | `thrash-detector` limitation 2 | unscheduled (S2 state across sessions goes with full recursion) | lesson 05 → narrowed lessons 11, 12 |
| 15 | The model router's dispatcher is exercised only with a live model; no headless test drives failover end to end.  | `model-router` limitation 3 | [#46](https://github.com/MetaCoding-io/regulator/issues/46) (roadmap 0.2); was: the first live run (lesson 14 drill 2 under the packaged definition; with row 33) | lesson 07 → rescheduled lesson 14 → rescheduled lesson 14 → with row 33 (lesson 15) |
| ~~16~~ | ~~The recovery router counts occurrences per cause; a unit alternating between two causes never reaches either rule's third action and stops at the attempt ceiling. Whether that is the intended behaviour is undecided.~~ Decided in lesson 11: by design. Per-cause occurrence is the policy's contract, the attempt ceiling is the global stop, and the alternation is visible in the unit's decisions and obligations; a policy that wants a global count declares shorter rules. The card states it as a limit, not debt. | `recovery-router` limitation 3 | — | lesson 08 → decided lesson 11 |
| ~~17~~ | ~~A protected change made *and committed* by one shell command survives the bash watch: the working tree is restored, the commit is not, and the closeout gate's `protected-untouched` check reads `git status`, which is clean.~~ Paid in lesson 12: the `identity-untouched` host check diffs the unit's branch against its base under every protected prefix at closeout; the workload's implement, verify and integrate unit types name it. | `identity-write-gate` limitation 1; `identity-untouched-check` | — | lesson 10 → paid lesson 12 |
| ~~18~~ | ~~The dispatcher's loader refuses the project's extensions, skills, prompts and themes, but still reads the project's context files and `.pi/settings.json`.~~ Paid in lesson 12: the loader takes no context files from the worktree, and the session runs on the definition's `settings.json` held in memory; the identity a unit sees is checkpoint 11's section. | `project-trust-rule` limitation 2 | — | lesson 10 → paid lesson 12 |
| 19 | Nothing watches network egress; the canary watch sees tool results and assistant text only. Pi's security doc names the container as the boundary. | `canary-watch` limitation 3 | unscheduled (containment is the operator's) | lesson 10 |
| ~~20~~ | ~~An obligation owed to a person is exposed by the read model and the control room, not delivered: nothing notifies, nothing reminds, and the CLI trusts `--by` — who may accept a risk at what severity is not checked and no dialog asks anyone.~~ Paid in lesson 13: what is owed to a person is delivered to the outbox through the effect journal and reminded after the interaction policy's interval; `ask_human` asks through the session's dialogs with the policy's timeout per kind and pauses the unit when no answer comes; every `--by` is checked against the policy's people before anything is written. The channel beyond the outbox and authentication of the name are rows 27 and 28. | `algedonic-delivery`; `interaction-contract`; `disposition-authority`; `obligation-router` limitations 2 and 4; `recovery-router` limitation 4 | — | lesson 11 → paid lesson 13 |
| ~~21~~ | ~~A proposal is an obligation owed to S5, but the S5 decision itself has no workflow.~~ Paid in lesson 12: `regulator identity accept|reject` is the only writer of an identity file — S5 authority, an invalid result reverted, a commit citing the obligation, the obligation resolved as accepted or rejected. The proposal's text is turned into a file by a person. | `proposal-intake` limitation 1; `s5-decision` | — | lesson 11 → paid lesson 12 |
| ~~22~~ | ~~Effective severity is the message's own (uncertainty impact aside); a message that names a protected invariant is not raised by that fact.~~ Paid in lesson 12: the routing policy declares floors — patterns over what a message says — and the lab's raises anything naming an invariant id or the identity path to blocking. | `obligation-router` limitation 3 | — | lesson 11 → paid lesson 12 |
| 23 | Routing runs at the loop's steps and on `regulator signals route`; a session run by hand leaves its messages unrouted until then. Nothing watches the log. | `obligation-router` limitation 1 | [#59](https://github.com/MetaCoding-io/regulator/issues/59) (roadmap 0.3); was: unscheduled (a file trigger is a lesson 14/15 operating concern) | lesson 11 |
| ~~24~~ | ~~An identity decision changes the instance's copy under `regulator/identity/`, not the definition's seed under `packages/regulator/identity/`; the next fixture starts from the seed. Promoting a decision into the definition is a commit by hand.~~ Paid in lesson 15: `regulator identity promote <file>.md` copies the instance's accepted identity file into the definition's seed, refusing a no-op, an invalid result or a dirty definition, and commits in the definition's repository under S5 authority; every later `init` starts from it. | `identity-promotion`; `s5-decision` limitation 3 | — | lesson 12 → paid lesson 15 |
| 25 | The identity rendered into a unit's prompt is truncated at 6000 characters, and a set longer than that is partly advice the model never sees; nothing warns. | `identity-context` limitation 3 | [#51](https://github.com/MetaCoding-io/regulator/issues/51) (roadmap 0.2); was: unscheduled (the set is meant to stay small; a warning is cheap when it is not) | lesson 12 |
| 26 | Operational memory is rendered to every unit of the instance, unscoped, and nothing checks a fact is true; the 90-day review limit is the only pressure. Lesson 14 measures the hygiene (the `memoryRules` and `memoryFacts` graders) and shows the tool changes what a unit can do, not what a prose-preferring unit will do; scoping and truth are unpaid. Lesson 15 scopes a fact to unit types (`remember`'s `scope`; the section is filtered by the unit's type); whether a fact is true stays a person's review. | `memory-store` limitations 1 and 2 | unscheduled (a truth check is a person's review; the expiry is the pressure) | lesson 12 → narrowed lesson 14 → narrowed lesson 14 → narrowed lesson 15 |
| ~~27~~ | ~~Delivery of what is owed to a person ends at the outbox, a file in the instance; no channel reaches a person who does not read it, and reminders run only at the loop's steps and on `regulator remind` — nothing schedules them in a quiet instance.~~ Paid in lesson 15: `regulator watch` delivers, reminds and forwards every new outbox line to the channel command the deployment names, with a cursor so a restart forwards nothing twice; `--once` is the CI form. Supervising the process and choosing the channel are the deployment's (row 38). | `outbox-watcher`; `algedonic-delivery` limitations 1 and 2 | — | lesson 13 → paid lesson 15 |
| 28 | `--by` is asserted, not authenticated: disposition authority checks whether the named person may, not whether the caller is that person; the dialog path attributes an answer to the session's OS user without the check. | `disposition-authority` limitations 1 and 3; `interaction-contract` limitation 1 | unscheduled (authentication belongs to the deployment; the lab records the claim honestly) | lesson 13 |
| 29 | Whether an action needs consent is the model's to notice: the profile's advice says when, the tool contracts what consent means, and nothing derives "this is irreversible" from a tool's declared effects before it runs.  | `interaction-contract` limitation 2 | [#60](https://github.com/MetaCoding-io/regulator/issues/60) (roadmap 0.3); was: unscheduled (an effect-derived consent check would make every `notify_owner` a consent question; the profile's advice and the tool's contract are the mechanism until a per-tool consent grant is a profile field) | lesson 13 → rescheduled lesson 13 → unscheduled lesson 15 |
| 30 | A paused unit holds its lease and worktree indefinitely; nothing expires the wait, and a question nobody answers is visible but never times out at the loop's level. | `pause-gate` limitation 3 | [#52](https://github.com/MetaCoding-io/regulator/issues/52) (roadmap 0.2); was: unscheduled (a wait ceiling is a policy field the routing or interaction policy could declare; the honest default is to wait) | lesson 13 |

| ~~31~~ | ~~The SQLite regulatory event store the reporting tools write (`.gsd/vsm-runtime/vsm.db`) is the Pi extension's, not the lab instance's: the span projection does not read it, and its path still carries a GSD-era name.~~ Paid in lesson 15: the store is `.regulator/events.db`, beside the instance's other records, and the span projection reads its rows as events on the unit their provenance names. | `span-projection` limitation 3; `docs/REPORTING.md` | — | lesson 14 → paid lesson 15 |
| ~~32~~ | ~~Review dates are checked on demand (`regulator review --due`); nothing under CI fails on an overdue record, so a card can pass its review date unnoticed.~~ Paid in lesson 15: `checkRegistry` takes today's date and an overdue review is a problem; `regulator check` (under `pnpm check`, so under CI) and `regulator doctor` both fail on it. | `regulator-lifecycle` limitation 2; `doctor` | — | lesson 14 → paid lesson 15 |
| 33 | The committed eval reports ran scripted units: the session gates (profile grant, write gate, bash watch, canary watch, budget guard) were not exercised, only the loop and the closeout checks, and the numbers are the graders' validation, not evidence about a model. A live report over the drift suite is owed. | `eval-harness` limitation 1; the reports' fingerprints | [#46](https://github.com/MetaCoding-io/regulator/issues/46) (roadmap 0.2); was: the first live run (lesson 14 drill 2; unscheduled under CI, since it needs a model and spends) | lesson 14 |
| ~~34~~ | ~~Vocabulary drift and rules written into prose are measured by the graders and absorbed by nothing: the sloppy report shows them identical in both arms. A glossary lint at closeout is the obvious check and is not declared.~~ Paid in lesson 15: the identity's glossary names the words this instance does not use, `glossary-lint` reads added comment lines and commit messages on the branch for them at closeout, and the drift suite's treatment arm declares it. The regenerated sloppy report shows the cost — a correct unit refused three times for a word in a commit message — which is row 36. | `glossary-lint`; `eval-harness` limitation 4 | — | lesson 14 → paid lesson 15 |
| 35 | `export-signature` observes `Function.length`, which stops at the first defaulted parameter: a defaulted second argument passes as a one-argument signature. One kind of behaviour check exists; return types and behaviour under input are not observed. | `behaviour-check` limitation 1 | [#54](https://github.com/MetaCoding-io/regulator/issues/54) (roadmap 0.2); was: unscheduled (a richer probe is a workload's to ask for; lesson 14 drill 4 collects the answers) | lesson 14 |
| 36 | `glossary-lint` is blocking wherever the workload declares it, and it reads commit messages as well as comments: one refused word in a commit message costs an attempt, and the regenerated sloppy report shows a correct unit refused three times for it. The attenuation — comment half blocking, commit-message half advisory — is a workload's declaration to make, and none makes it. | `glossary-lint` limitation 2; the sloppy report's interpretation | [#47](https://github.com/MetaCoding-io/regulator/issues/47) (roadmap 0.2); was: unscheduled (lesson 15 drill 3 collects the attenuated report; the check gains a severity when a workload asks) | lesson 15 |
| 37 | The post-merge check runs after the merge, not before: the base is red for as long as S3 takes to decide. A pre-merge trial on a temporary merge commit would refuse the reintegration instead, at the cost of a second worktree per close. | `post-merge-check` limitation 1 | [#48](https://github.com/MetaCoding-io/regulator/issues/48) (roadmap 0.2); was: unscheduled (lesson 15 drill 2 asks whether the cost is worth paying) | lesson 15 |
| 38 | The outbox watcher is a process, not a service: nothing restarts it, one cursor serves one instance, and a channel that accepts a line and loses it downstream is never retried. Supervision and the channel are the deployment's. | `outbox-watcher` limitations 1–3 | unscheduled (a systemd unit or a CI schedule; documented in OPERATING.md) | lesson 15 |
| 39 | An instance's manifest records the definition revision at init; `doctor` reports drift from it, but nothing migrates an instance to a newer definition and the manifest is never rewritten. The upgrade path is a person reading OPERATING.md. | `instance-manifest` limitation 2; `doctor` limitation 2 | [#53](https://github.com/MetaCoding-io/regulator/issues/53) (roadmap 0.2); was: unscheduled (a definition version per promotion, and a migration note per version, when the definition is released as a package) | lesson 15 |
| 40 | The planner-quality and contract-drift diagnostics (undelegated consequential decisions discovered during execution, fixed-decision deviations, delegated-decision completion, planned versus emergent uncertainty, replans attributable to the contract) are recorded in every result report — `delegatedResults`, `unresolvedOutcomes`, `emergentDecisions`, `deviations`, `residualUncertainty` — and measured by nothing: the trajectory graders count refusals, retries, escalations and invariant violations only, so nobody can say whether planning is allocating variety well. | `eval-harness` limitation 4; `packages/regulator/src/graders.ts`; issue #12 (closed into this row) | [#49](https://github.com/MetaCoding-io/regulator/issues/49) (roadmap 0.2); was: unscheduled (a small grader extension in lesson 14's harness: read the reports from the execution store, pre-register the metrics in the suite, and let the eval report carry them per arm) | lesson 14 |
| 41 | `inherited-tests` counts test and assertion lines and matches regressions by test name: a kept assertion rewritten to the current output in a file the base's copy already failed on is neither a shrink nor a regression, and a file the contract exempts is not seen at all. The base's package.json is staged with the base's tests, so a unit that legitimately adds a test dependency fails until the contract exempts it. Only `test/` is inherited. | `inherited-tests-check` limitations 1–3 | [#50](https://github.com/MetaCoding-io/regulator/issues/50) (roadmap 0.2); was: unscheduled (a per-assertion diff against the base's copy, and a test-layout convention from the manifest, when a workload asks; the base-side run could be cached per base revision) | after lesson 15 |

Permanent limits, listed so nobody re-files them as debt: prompt sections are advice and
can be ignored (`contract-advice`); a model summary can be wrong and only the
deterministic block is guaranteed (`contract-preserving-compaction`); the evidence
preflight trusts the session's own tool run by design, and independence is the closeout
gate's (`evidence-preflight`); an effect with no observable trace can only be
reconciled as absent (`effect-journal`); the gate reads the report, never the diff
(`result-report-gate`).
